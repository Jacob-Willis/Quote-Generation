# Quote-Generation
Generate a list of random quotes to display onto the browser

Run in visual studio with Blazer

Need to add a button to fetch list of quotes from json and randomly select one to display on UI

Code is in "master" branch not "main". You will need to "git checkout master" to access the correct code
